meta:
  id: redis
  name: Redis
  type: Platform
  category: Database
  description: "Connects to Redis instances for high-speed caching and state management."
  version: "1.0.0"

config:
  driver: redis

settings:
  - name: connection
    label: "Redis Connection"
    type: connection_select
    required: true
