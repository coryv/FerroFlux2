meta:
  id: postgresql.tx.rollback
  name: Rollback Transaction
  category: Transaction
  type: Action
  platform: postgresql
  description: "Aborts the active database transaction for the current execution flow."
  version: "1.0.0"

interface:
  inputs:
    - name: Exec
      type: flow
  outputs:
    - name: Success
      type: flow
    - name: Error
      type: flow

execution:
  - id: rollback
    tool: sql_query
    params:
      operation: rollback
      connection_string: "{{ get 'settings.connection_string' }}"
    returns:
      status: status

routing:
  match: success
  cases:
    success:
      - tool: emit
        params:
          port: Success
