meta:
  id: postgresql.tx.begin
  name: Begin Transaction
  category: Transaction
  type: Action
  platform: postgresql
  description: "Starts a new database transaction for the current execution flow."
  version: "1.0.0"

interface:
  inputs:
    - name: Exec
      type: flow
  outputs:
    - name: Success
      type: flow
    - name: Error
      type: flow

execution:
  - id: begin
    tool: sql_query
    params:
      operation: begin
      connection_string: "{{ get 'settings.connection_string' }}"
    returns:
      status: status

routing:
  match: success
  cases:
    success:
      - tool: emit
        params:
          port: Success
