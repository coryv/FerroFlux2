meta:
  permissions: ["network:*.salesforce.com"]
  id: salesforce.records.delete
  name: Delete Record
  category: Records
  type: Action
  platform: salesforce
  description: "Deletes a standard or custom SObject record."
  version: "1.0.0"

interface:
  inputs:
    - name: Exec
      type: flow
    - name: sObjectName
      type: string
    - name: recordId
      type: string
  outputs:
    - name: Success
      type: flow
    - name: Error
      type: flow

execution:
  - id: request
    tool: http_client
    params:
      method: DELETE
      url: "{{ platform.base_url }}/sobjects/{{ get 'inputs.sObjectName' }}/{{ get 'inputs.recordId' }}"
      headers: "{{ platform.headers }}"
    returns:
      status: status_code
      body: response_body

routing:
  match: "{{ steps.request.status_code }}"
  cases:
    "204":
      - tool: emit
        params:
          port: Success
    default:
      - tool: emit
        params:
          port: Error
          value: "{{ steps.request.response_body }}"
