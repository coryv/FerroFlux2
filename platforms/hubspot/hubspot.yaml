meta:
  permissions: ["network:api.hubapi.com"]
  id: hubspot
  name: HubSpot
  type: Platform
  category: CRM
  description: "Connects to the HubSpot API."
  version: "1.0.0"

config:
  base_url: "https://api.hubapi.com/crm/v3"
  headers:
    Content-Type: "application/json"

settings:
  - name: connection
    label: "HubSpot Account"
    type: connection_select
    required: true
