meta:
  id: core
  name: Core
  category: Utilities
  type: Platform
  description: "Core FerroFlux Logic Nodes"
  version: "1.0.0"

config:
  # No shared config needed for core nodes usually, but structure is required.
  version: "1.0.0"
