meta:
  permissions: ["network:www.googleapis.com"]
  id: google_calendar.events.create
  name: Create Event
  category: Events
  type: Action
  platform: google_calendar
  description: "Creates an event."
  version: "1.0.0"

interface:
  inputs:
    - name: Exec
      type: flow
    - name: calendarId
      type: string
    - name: event_data
      type: object
  outputs:
    - name: Success
      type: flow
    - name: Error
      type: flow
    - name: event
      type: object
  settings: []

execution:
  - id: request
    tool: http_client
    params:
      method: POST
      url: "{{ platform.base_url }}/{{ get 'inputs.calendarId' }}/events"
      headers: "{{ platform.headers }}"
      body: "{{ get 'inputs.event_data' }}"
    returns:
      status: status_code
      body: response_body

  - id: check_status
    tool: switch
    params:
      value: "{{ steps.request.status_code }}"
      cases:
        - condition: "200"
          output: success
        - condition: default
          output: error

routing:
  match: "{{ steps.check_status.branch }}"
  cases:
    success:
      - tool: emit
        params:
          port: Success
      - tool: emit
        params:
          port: event
          value: "{{ steps.request.response_body }}"
    error:
      - tool: emit
        params:
          port: Error
          value: "{{ steps.request.response_body }}"
